// for loop to go through each index and splice "book#" and add to a new array
// for each can be used
// splice could be a string thats formatted seperately

// recursion to look for the length of the given array and return 

// array made of objects - objects use names to access its members = array.objectName

const getTheTitles = function(array) {
    return array.map((item) => item.title);
};

// Do not edit below this line
module.exports = getTheTitles;
